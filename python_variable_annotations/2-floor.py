#!/usr/bin/env python3
"""
2-floor
"""

import math


def floor(n: float) -> int:
    """
    Calculate the floor of a float.

    Args:
        n (float): the float to floor

    Returns:
        int: the largest integer <= n
    """
    return math.floor(n)
